# ── Quant Energy Commodity Pipeline — Python Dependencies ──────────────────
# pip install -r requirements.txt

# ── Core scientific stack ───────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0

# ── Machine learning (ML signal generation) ─────────────────────────────────
scikit-learn>=1.4.0

# ── Market data (live prices, options chains, OHLCV) ────────────────────────
yfinance>=0.2.40

# ── HTTP (FRED risk-free rate, API fallbacks) ────────────────────────────────
requests>=2.31.0

# ── Logging & data validation ───────────────────────────────────────────────
loguru>=0.7.0
pydantic>=2.0.0

# ── Global ecosystem additions ──────────────────────────────────────────────
xgboost>=2.0.0
beautifulsoup4>=4.12.0
fastapi>=0.110.0
uvicorn>=0.29.0
ib_insync>=0.9.86
anthropic>=0.25.0
python-dotenv>=1.0.0         # loads .env credentials at startup (file is gitignored)

# ── CrewAI trading team (crew_agent.py) ──────────────────────────────────────
crewai>=0.28.0               # 4-agent decentralized orchestration framework
langchain-anthropic>=0.1.0   # Claude Sonnet/Opus LLM backend (primary)
langchain-openai>=0.1.0      # GPT-4o fallback LLM + OpenAI embeddings for ChromaDB
langchain-community>=0.2.0   # Chroma vector store integration
chromadb>=0.4.0              # Local vector database for institutional knowledge base
pypdf>=4.0.0                 # PDF ingestion for institutional source documents
alpaca-trade-api>=3.0.0      # Alpaca paper trading execution (paper-api.alpaca.markets)
feedparser>=6.0.0            # EIA/Reuters/CNBC RSS live news feed ingestion

# ── Streamlit dashboard (dashboard.py) ───────────────────────────────────────
streamlit>=1.35.0            # Real-time portfolio telemetry UI
plotly>=5.20.0               # Greeks surface, latency profile, PnL curve charts

# ── Optional: faster number crunching ───────────────────────────────────────
# numba>=0.59.0          # JIT acceleration for Monte Carlo loops
# bottleneck>=1.3.7      # Fast numpy operations on arrays

# ── Development & testing ────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0

# ── Code quality ────────────────────────────────────────────────────────────
black>=24.0.0
ruff>=0.4.0
isort>=5.13.0
mypy>=1.9.0

# ── Type stubs for mypy ──────────────────────────────────────────────────────
pandas-stubs>=2.1.0
types-requests>=2.31.0

# ── Optional: Jupyter for interactive analysis ───────────────────────────────
# jupyterlab>=4.1.0
# ipywidgets>=8.1.0
# matplotlib>=3.8.0
# plotly>=5.20.0

# ── Optional: advanced volatility modelling ─────────────────────────────────
# arch>=6.3.0            # GARCH(1,1) MLE estimation (replaces moment-based)
